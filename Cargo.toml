[package]
name = "game-night"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rocket = { version = "=0.5.0-rc.3", features = ["secrets"] }
rocket_dyn_templates = { version = "=0.1.0-rc.3", features = ["tera"] }
