{
    "title": "Tau's Game Night{% if event.title %} «{{event.title}}»{% endif %} is set for {{event.starts_at | time(format = '{date}')}} 🥳",
    "body": "According to the poll, this is a date that you're not available on. If your plans change, you can always join us. 😊",
    "navigate": "{{event_page_uri(event_id=event.id)}}",
    "requireInteraction": true,
    "renotify": true,
    "tag": "event-{{event.id}}",
    "actions": [
        {
            "action": "saveToCalendar",
            "title": "Save to Calendar",
            "navigate": "{{event_ics_uri(event_id=event.id)}}"
        },
        {
            "action": "add",
            "title": "Add me to the Event...",
            "navigate": "{{event_page_uri(event_id=event.id)}}"
        }
    ]
}
