{
    "title": "You're Invited to Tau's Game Night{% if event.title %} «{{event.title}}»{% endif %}",
    "body": "The game night is happening on {{event.starts_at | time(format = '{date}')}} at {{event.starts_at | time(format = '{time}')}} and you are warmly invited {{random_heart()}}",
    "navigate": "{{event_page_uri(event_id=event.id)}}",
    "requireInteraction": true,
    "renotify": true,
    "tag": "event-{{event.id}}",
    "actions": [
        {
            "action": "saveToCalendar",
            "title": "Save to Calendar",
            "navigate": "{{event_ics_uri(event_id=event.id)}}"
        },
        {
            "action": "leave",
            "title": "Leave Event...",
            "navigate": "{{leave_event_uri(event_id=event.id)}}"
        }
    ]
}
