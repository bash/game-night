{
    "title": "New Poll for {% if !poll.event.title.is_empty() %}Tau's Game Night «{{poll.event.title}}»{% else %}Tau's Game Night{% endif %}",
    "body": "Help pick a date until until {{*poll.open_until | time(\"{date}\")}} at {{*poll.open_until | time(\"{time}\")}}",
    "navigate": "{{functions::event_page_uri(*poll.event.id)}}",
    "tag": "event-{{poll.event.id}}",
    "requireInteraction": true,
    "actions": [
        {
            "action": "skip",
            "title": "Skip Poll...",
            "navigate": "{{functions::skip_poll_uri(*poll.event.id)}}"
        }
    ]
}
